/**
 * Analytics Module — Local-first analytics
 *
 * All user data is stored locally via Local_Store / Local_Tracker.
 * The old cloud-first unified-tracker, unified-analytics-client, and
 * cloudflare-client have been removed.
 *
 * Usage:
 *   import { AnalyticsProvider } from '@/lib/analytics';
 */

export { AnalyticsProvider } from './AnalyticsProvider';
